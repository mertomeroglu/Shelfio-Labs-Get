import type { Metadata } from "next";
import type { ReactNode } from "react";

export const metadata: Metadata = {
  title: "Lisanslarım",
};

export default function AccountLicensesLayout({ children }: { children: ReactNode }) {
  return children;
}
